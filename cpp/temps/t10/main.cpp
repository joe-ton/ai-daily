#include <iostream>
#include <vector>
#include <unordered_map>

std::vector<int> getResponse(const std::vector<int>& nums, int target) {
}

int main() {
    int target = 7;
    std::vector<int> nums = {1, 2, 3, 4};

    auto result = getResponse(nums, target);

    std::cout << "Result: ";
    for (const auto& )
}
