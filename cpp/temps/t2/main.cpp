#include <iostream>
#include <vector>

// input: 1-index array of integers, sorted non-decreasing (increasing)
// ex. {1, 2, 3, 4}
// ex. [1, 2, 3, 4]
// 1-index, target

// can only use constant-space ex. cannot hashmap
// cannot use elements more than twice -> iterate through once

pair<int, int> run(vector<int>& nums, int target) {
    int left;
    int right;

    for (left < right) {

        left++;
        right--;
    }
}

int main() {

}
