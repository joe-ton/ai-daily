#include <iostream>
#include <vector>
#include <unordered_map>

std::vector<int> getResponse(const std::vector<int>& vector)

int main() {
    int target = 7;
    std::vector<int> = {1, 2, 3, 4};
    auto result = getResponse(vector, target);
}

