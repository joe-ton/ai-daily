#include <iostream>
#include <vector>
#include <unordered_map>

int main() {
    std::vector<int> nums = {1, 2, 3, 4};
    int target = 7;

    auto result = run(nums, target);

    if (result.first != -1) {

    }
}
