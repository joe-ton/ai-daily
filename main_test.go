package main

import (
    "testing"
)

func TestMapSolver_TwoSum(t *testing.T) {
    solver := MapSolver{}

    tests := []struct {
        Name string
        nums []int
        target int
        want ([]int, error)
    } {
        {

        },
    }

    for tt := range tests {
        t.Run(t.Name, func(t *testing.T) {

        })
    }
}
