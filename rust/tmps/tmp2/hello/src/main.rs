use std::collections::HashMap;

fn run(nums: vec![i32], target: i32) -> Result<(usize, usize)> {
    let seen: HashMap<i32, i32> = HashMap::new();

    for idx, num in nums.iter().enumerate() {

    }
}


fn main() {
    let target = 7;
    let nums = [1, 2, 3, 4];

    let result = run(nums, target);
}
