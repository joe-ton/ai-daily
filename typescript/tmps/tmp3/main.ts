function main() {
    const nums = [1, 2, 3, 4];
    const target = 7;
    const resp = two_sum(nums, target);
    console.log(resp)
}

function two_sum(nums: number[], target: number): [number, number] | null{
    const seen = new Map<number, number>();

    for (let i = 0; i < nums.length(), i++) {
        const num = nums[i];
        const complement = taget - num;

        if (seen.has())

    }
    return null
}
